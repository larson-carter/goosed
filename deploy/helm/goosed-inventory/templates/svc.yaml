apiVersion: v1
kind: Service
metadata:
  name: {{ include "goosed-inventory.fullname" . }}
  labels:
    {{- include "goosed-inventory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "goosed-inventory.selectorLabels" . | nindent 4 }}
