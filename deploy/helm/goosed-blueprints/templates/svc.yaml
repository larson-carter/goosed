apiVersion: v1
kind: Service
metadata:
  name: {{ include "goosed-blueprints.fullname" . }}
  labels:
    {{- include "goosed-blueprints.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "goosed-blueprints.selectorLabels" . | nindent 4 }}
