apiVersion: v1
kind: Service
metadata:
  name: {{ include "goosed-orchestrator.fullname" . }}
  labels:
    {{- include "goosed-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "goosed-orchestrator.selectorLabels" . | nindent 4 }}
